# TriageAI — Python Dependencies (requires Python 3.11)
# Install: conda create -n triageai python=3.11 && conda activate triageai && pip install -r requirements.txt

# ML Pipeline
xgboost==1.7.6
scikit-learn==1.3.2
pandas==2.0.3
numpy==1.24.3
shap==0.42.1
optuna==3.4.0
imbalanced-learn==0.11.0
joblib==1.3.2
matplotlib==3.7.2
seaborn==0.12.2

# Flask Backend
flask==2.3.3
flask-jwt-extended==4.5.3
flask-sqlalchemy==3.0.5
flask-migrate==4.0.5
flask-cors==4.0.0
flask-limiter==3.5.0
psycopg2-binary==2.9.7
marshmallow==3.20.1
werkzeug==2.3.7
gunicorn==21.2.0

# Experiment Tracking (optional — train_model.py degrades gracefully if absent)
mlflow==2.9.2

# Caching (optional — falls back to in-process cache if REDIS_URL unset/redis absent)
# redis==5.0.1

# Model-serving optimization (optional — scripts/export_onnx.py degrades gracefully)
# skl2onnx==1.16.0
# onnxmltools==1.12.0
# onnxruntime==1.17.0

# Load testing (optional — see jmeter/README.md)
# locust==2.24.0

# Testing
pytest==7.4.2
pytest-cov==4.1.0
httpx==0.25.0
